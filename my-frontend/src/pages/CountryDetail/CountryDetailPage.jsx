import AlertListPage from "../../components/AlertListPage/AlertListPage";

const CountryDetailPage = ({ title, data }) => {
  return <AlertListPage title={title} data={data} />;
};

export default CountryDetailPage;
